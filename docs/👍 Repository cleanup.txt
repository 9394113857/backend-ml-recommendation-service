git rm -r --cached venv
git rm -r --cached venv_old
git rm -r --cached __pycache__
git rm -r --cached *.pyc

git add .gitignore
git add .

git commit -m "Final cleanup: gitignore + remove venv & cache"
git push origin main