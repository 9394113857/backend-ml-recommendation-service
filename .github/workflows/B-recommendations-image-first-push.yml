name: 🚀 Build & Push First ---> backend-ml-recommendation-service image

on:
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:

      # ==============================
      # 📥 Checkout Repository
      # ==============================
      - name: 📥 Checkout Code
        uses: actions/checkout@v3

      # ==============================
      # 🔐 Docker Login
      # ==============================
      - name: 🔐 Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # ==============================
      # 🧠 Show Info
      # ==============================
      - name: 🧠 Show Build Info
        run: |
          echo "🚀 Starting Docker Build for Recommendations Service"
          echo "📦 Image: pythoncodelife/backend-ml-recommendation-service:latest"
          echo "🕒 Time: $(date)"

      # ==============================
      # 🐳 Build Docker Image
      # ==============================
      - name: 🐳 Build Docker Image
        run: |
          docker build -t pythoncodelife/backend-ml-recommendation-service:latest .

      # ==============================
      # 📤 Push Docker Image
      # ==============================
      - name: 📤 Push Docker Image
        run: |
          docker push pythoncodelife/backend-ml-recommendation-service:latest

      # ==============================
      # 🔍 Verify Image
      # ==============================
      - name: 🔍 Verify Image Push
        run: |
          echo "✅ Image pushed successfully!"
          echo "🔗 DockerHub: https://hub.docker.com/r/pythoncodelife/backend-ml-recommendation-service"
      
      # ==============================
      # 🎉 Done
      # ==============================
      - name: 🎉 Done
        run: |
          echo "🚀 backend-ml-recommendation-service Docker Image Ready for Deployment"
